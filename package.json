{
  "name": "node-package-api",
  "version": "1.0.0",
  "description": "Run packages in their own process",
  "main": "node-package-api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Iedema",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}
